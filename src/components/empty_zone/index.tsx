import {Box} from "@mui/material";
import Typography from "@mui/material/Typography";

export default function EmptyZone(){
    return (
        <Box>
            <Typography variant='h5'>Select a zone on the left</Typography>
        </Box>
    );
}